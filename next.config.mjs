/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : [
            'res.cloudinary.com',
            'another-domain.com'
        ],
    },
};

export default nextConfig;

